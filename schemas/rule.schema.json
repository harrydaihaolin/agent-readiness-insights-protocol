{
  "$defs": {
    "CommandInMakefileMatch": {
      "additionalProperties": false,
      "properties": {
        "fire_when": {
          "default": "missing",
          "enum": [
            "missing",
            "present"
          ],
          "title": "Fire When",
          "type": "string"
        },
        "target": {
          "description": "Makefile target name, e.g. 'test'.",
          "title": "Target",
          "type": "string"
        },
        "type": {
          "const": "command_in_makefile",
          "default": "command_in_makefile",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "target"
      ],
      "title": "CommandInMakefileMatch",
      "type": "object"
    },
    "CompositeMatch": {
      "additionalProperties": false,
      "description": "Boolean composition over OSS leaf match clauses.\n\nEach leaf clause is \"satisfied\" when its matcher returns no\nfindings (the required paths exist, the required regex matches,\nthe manifest field is set, \u2026). Composite ops are defined over\nthat per-clause satisfied / unsatisfied bit:\n\n- ``and``: composite is satisfied iff every clause is satisfied.\n  When any clause is unsatisfied, emits one composite finding\n  summarising which conjunct(s) failed (callers typically use\n  ``summary`` to carry the human-readable conjunction text).\n- ``or``: composite is satisfied iff at least one clause is\n  satisfied. When ALL clauses are unsatisfied, emits one finding\n  per failing clause so the user sees each unmet alternative,\n  prefixed by ``summary`` (if set) so the reporter can label the\n  all-fail case explicitly.\n- ``not``: composite is satisfied iff the (single) clause is\n  unsatisfied. Used to express \"X is missing\"; emits one finding\n  naming what ``X`` was when the inner clause was satisfied.\n\nNote on ``or``: an earlier revision of this docstring described\n``or`` as \"fires when any clause produces \u22651 finding\" (i.e.\nOR-of-failures). That wording was inverted relative to how every\nrule in the agent-readiness rules pack uses ``or``  \u2014 \"any of\nthese alternatives is acceptable\" \u2014 and was producing 6+\nspurious findings per scanned repo. The semantic above\n(OR-of-satisfactions) is the corrected, canonical contract.\n\nNested ``CompositeMatch`` clauses are allowed up to a small depth\n(engines should cap recursion at 4 to keep evaluation predictable).\n``PrivateMatch`` clauses are also accepted so composite expressions\ncan incorporate downstream-engine analyses.",
      "properties": {
        "clauses": {
          "items": {
            "anyOf": [
              {
                "$ref": "#/$defs/FileSizeMatch"
              },
              {
                "$ref": "#/$defs/PathGlobMatch"
              },
              {
                "$ref": "#/$defs/ManifestFieldMatch"
              },
              {
                "$ref": "#/$defs/RegexInFilesMatch"
              },
              {
                "$ref": "#/$defs/CommandInMakefileMatch"
              },
              {
                "$ref": "#/$defs/CompositeMatch"
              },
              {
                "$ref": "#/$defs/PrivateMatch"
              }
            ]
          },
          "minItems": 1,
          "title": "Clauses",
          "type": "array"
        },
        "op": {
          "enum": [
            "and",
            "or",
            "not"
          ],
          "title": "Op",
          "type": "string"
        },
        "summary": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional human-readable summary used as the composite finding message.",
          "title": "Summary"
        },
        "type": {
          "const": "composite",
          "default": "composite",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "op"
      ],
      "title": "CompositeMatch",
      "type": "object"
    },
    "FileSizeMatch": {
      "additionalProperties": false,
      "properties": {
        "exclude_globs": {
          "items": {
            "type": "string"
          },
          "title": "Exclude Globs",
          "type": "array"
        },
        "threshold_bytes": {
          "default": 51200,
          "minimum": 1,
          "title": "Threshold Bytes",
          "type": "integer"
        },
        "threshold_lines": {
          "default": 500,
          "minimum": 1,
          "title": "Threshold Lines",
          "type": "integer"
        },
        "type": {
          "const": "file_size",
          "default": "file_size",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "FileSizeMatch",
      "type": "object"
    },
    "ManifestFieldMatch": {
      "additionalProperties": false,
      "properties": {
        "field_path": {
          "description": "Dotted path to a required field, e.g. 'project.scripts'.",
          "title": "Field Path",
          "type": "string"
        },
        "fire_when": {
          "default": "missing",
          "enum": [
            "missing",
            "present"
          ],
          "title": "Fire When",
          "type": "string"
        },
        "manifest": {
          "description": "Manifest filename, e.g. 'pyproject.toml' or 'package.json'.",
          "title": "Manifest",
          "type": "string"
        },
        "type": {
          "const": "manifest_field",
          "default": "manifest_field",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "manifest",
        "field_path"
      ],
      "title": "ManifestFieldMatch",
      "type": "object"
    },
    "PathGlobMatch": {
      "additionalProperties": false,
      "properties": {
        "forbid_globs": {
          "items": {
            "type": "string"
          },
          "title": "Forbid Globs",
          "type": "array"
        },
        "require_globs": {
          "items": {
            "type": "string"
          },
          "title": "Require Globs",
          "type": "array"
        },
        "type": {
          "const": "path_glob",
          "default": "path_glob",
          "title": "Type",
          "type": "string"
        }
      },
      "title": "PathGlobMatch",
      "type": "object"
    },
    "Pillar": {
      "description": "Mirrors agent_readiness.models.Pillar so the schema stays in sync.",
      "enum": [
        "cognitive_load",
        "feedback",
        "flow",
        "safety"
      ],
      "title": "Pillar",
      "type": "string"
    },
    "PrivateMatch": {
      "additionalProperties": true,
      "description": "Catch-all for non-OSS (private) match types.\n\nDownstream engines (e.g. ``agent-readiness-pro`` or the closed\ninsights engine) extend the rules pack with bespoke analyses such as\n``git_log_query``, ``ast_complexity``, or ``regex_secret_scan``.\nThese are registered in the engine via\n``register_private_matcher(type_name, fn)`` and are referenced from\nYAML rules as ``match.type: <name>``.\n\nThe protocol does not specify the *shape* of a PrivateMatch \u2014 every\nfield beyond ``type`` is engine-specific \u2014 so this model accepts\narbitrary extra fields. ``type`` is constrained only to forbid the\nsix OSS match-type names, preventing a malformed OSS rule from\nsilently being parsed as a private one. Engines that do not know\na given private type produce a ``not_measured`` finding rather\nthan crashing.",
      "properties": {
        "type": {
          "description": "Engine-specific match type name; must NOT collide with an OSS type.",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "title": "PrivateMatch",
      "type": "object"
    },
    "RegexInFilesMatch": {
      "additionalProperties": false,
      "properties": {
        "case_insensitive": {
          "default": false,
          "title": "Case Insensitive",
          "type": "boolean"
        },
        "file_globs": {
          "items": {
            "type": "string"
          },
          "title": "File Globs",
          "type": "array"
        },
        "fire_when": {
          "default": "match",
          "enum": [
            "match",
            "no_match"
          ],
          "title": "Fire When",
          "type": "string"
        },
        "pattern": {
          "description": "Python re-style regex.",
          "title": "Pattern",
          "type": "string"
        },
        "type": {
          "const": "regex_in_files",
          "default": "regex_in_files",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "pattern"
      ],
      "title": "RegexInFilesMatch",
      "type": "object"
    },
    "Severity": {
      "enum": [
        "info",
        "warn",
        "error"
      ],
      "title": "Severity",
      "type": "string"
    }
  },
  "$id": "https://github.com/harrydaihaolin/agent-readiness-insights-protocol/schemas/rule.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Declarative rule. Loaded from YAML; produced as JSON by `/rules`.\n\n`rules_version` is the schema version of THIS rule file; loaders\nrefuse rules whose rules_version is outside their supported range.",
  "properties": {
    "explanation": {
      "default": "",
      "description": "Why this rule predicts an agent failure mode.",
      "title": "Explanation",
      "type": "string"
    },
    "fix_hint": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Fix Hint"
    },
    "id": {
      "description": "Stable identifier, e.g. 'repo_shape.large_files'.",
      "title": "Id",
      "type": "string"
    },
    "insight_query": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Free-text query the engine uses to retrieve related insights for findings of this rule.",
      "title": "Insight Query"
    },
    "match": {
      "anyOf": [
        {
          "$ref": "#/$defs/FileSizeMatch"
        },
        {
          "$ref": "#/$defs/PathGlobMatch"
        },
        {
          "$ref": "#/$defs/ManifestFieldMatch"
        },
        {
          "$ref": "#/$defs/RegexInFilesMatch"
        },
        {
          "$ref": "#/$defs/CommandInMakefileMatch"
        },
        {
          "$ref": "#/$defs/CompositeMatch"
        },
        {
          "$ref": "#/$defs/PrivateMatch"
        }
      ],
      "title": "Match"
    },
    "pillar": {
      "$ref": "#/$defs/Pillar"
    },
    "rules_version": {
      "default": 1,
      "description": "Schema version this rule conforms to. Loaders gate on this.",
      "title": "Rules Version",
      "type": "integer"
    },
    "severity": {
      "$ref": "#/$defs/Severity",
      "default": "warn"
    },
    "title": {
      "title": "Title",
      "type": "string"
    },
    "weight": {
      "default": 1.0,
      "maximum": 10.0,
      "minimum": 0.0,
      "title": "Weight",
      "type": "number"
    }
  },
  "required": [
    "id",
    "pillar",
    "title",
    "match"
  ],
  "title": "Rule",
  "type": "object"
}
